<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.joiest.jpf.dao.repository.mapper.custom.PayShopIncomeConfirmationMapper">
    <resultMap id="BaseResultMap" type="com.joiest.jpf.common.custom.PayShopOrderCustom">
        <id column="id" jdbcType="BIGINT" property="id" />
        <result column="order_no" jdbcType="VARCHAR" property="orderNo" />
        <result column="product_name" jdbcType="VARCHAR" property="productName" />
        <result column="product_money" jdbcType="DECIMAL" property="productMoney" />
        <result column="amount" jdbcType="INTEGER" property="amount" />
        <result column="total_money" jdbcType="DECIMAL" property="totalMoney" />
        <result column="pay_way" jdbcType="TINYINT" property="payWay" />
        <result column="status" jdbcType="TINYINT" property="status" />
        <result column="addtime" jdbcType="TIMESTAMP" property="addtime" />
        <collection property="shopIncomeConfirmationDetailResponseList" ofType="com.joiest.jpf.common.custom.ShopIncomeConfirmationDetailResponse">
            <result column="dou" jdbcType="DECIMAL" property="dou"/>
            <result column="coupon_no" jdbcType="VARCHAR" property="couponNo"/>
            <result column="coupon_banlance" jdbcType="DECIMAL" property="couponBanlance"/>
            <result column="contract_no" jdbcType="VARCHAR" property="contractNo"/>
            <result column="merch_order_no" jdbcType="VARCHAR" property="merchCouponOrderNo"/>
            <result column="contract_surplus" jdbcType="DECIMAL" property="merchCouponOrderBanlance"/>
            <result column="company_name" jdbcType="VARCHAR" property="companyName"/>
            <result column="rate" jdbcType="DECIMAL" property="rate"/>
        </collection>
    </resultMap>
    
    <sql id="Example_Where_Clause_Custom">
        <!--
          @mbggenerated
          This element is automatically generated by MyBatis Generator,Do not modify ! Generated on Fri Aug 03 13:59:18 CST 2018.
        -->
        <where>
            <foreach collection="oredCriteria" item="criteria" separator="or">
                <if test="criteria.valid">
                    <trim prefix="(" prefixOverrides="and" suffix=")">
                        <foreach collection="criteria.criteria" item="criterion">
                            <choose>
                                <when test="criterion.noValue">
                                    and a.${criterion.condition}
                                </when>
                                <when test="criterion.singleValue">
                                    and a.${criterion.condition} #{criterion.value}
                                </when>
                                <when test="criterion.betweenValue">
                                    and a.${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                                </when>
                                <when test="criterion.listValue">
                                    and a.${criterion.condition}
                                    <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                                        #{listItem}
                                    </foreach>
                                </when>
                            </choose>
                        </foreach>
                    </trim>
                </if>
            </foreach>
        </where>
    </sql>
    
    <select id="selectShopOrderCouponDetailList" parameterType="com.joiest.jpf.common.custom.PayShopOrderCustomExample" resultMap="BaseResultMap">
        select
            a.id,
            a.order_no,
            a.addtime,
            a.product_name,
            a.product_money,
            a.amount,
            a.total_money,
            a.pay_way,
            a.status,
            b.dou,
            b.coupon_no,
            c.coupon_dou_left + c.sale_dou_left coupon_banlance,
            d.contract_no,
            e.order_no as merch_order_no,
            b.contract_surplus,
            b.company_name,
            f.rate
        from
            pay_shop_order a
        LEFT JOIN
            pay_shop_coupon_active b
        ON
            b.order_id = a.id
        LEFT JOIN
            pay_shop_coupon_remain c
        ON
            b.coupon_no = c.coupon_no
        LEFT JOIN
            pay_shop_batch d
        ON
            b.batch_id = d.id
        LEFT JOIN
            pay_shop_coupon_order e
        ON
            d.order_id = e.id
        LEFT JOIN
            pay_shop_company_charge f
        ON
            d.company_charge_id = f.id
        <if test="_parameter != null">
            <include refid="Example_Where_Clause_Custom" />
        </if>
        ORDER BY
            a.id DESC
    </select>
</mapper>